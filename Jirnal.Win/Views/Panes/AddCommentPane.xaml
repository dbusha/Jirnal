<UserControl x:Class="Jirnal.Win.Views.Panes.AddCommentPane"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
             xmlns:viewModels="clr-namespace:Jirnal.Win.ViewModels"
             mc:Ignorable="d"
             d:DesignHeight="300" d:DesignWidth="300"
             d:DataContext="{d:DesignInstance Type=viewModels:IssueVm}">
    <Grid>
        <DockPanel>
            <Button DockPanel.Dock="Top" HorizontalAlignment="Right" Content="New Window" Click="OnOpenCommentWindow_"/>
            <StackPanel Orientation="Horizontal" DockPanel.Dock="Bottom" HorizontalAlignment="Right" Margin="20,0">
                <Button Content="Cancel" Command="{Binding CancelCommentCmd}"  Margin="5,3" Width="100"/>
                <Button Content="Add" Command="{Binding AddCommentCmd}" Margin="5,3" Width="100"/>
            </StackPanel>
            <TextBox Text="{Binding CommentText}" TextWrapping="Wrap" AcceptsReturn="True"
                     HorizontalScrollBarVisibility="Auto" MinHeight="150"/>
        </DockPanel>
    </Grid>
</UserControl>
